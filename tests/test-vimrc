" Mini .vimrc for testing via Vader. Run as follows:
" vim -u tests/test-vimrc tests/vader.vader

set nocompatible
filetype off

" Assumes that plugins are installed under ~/.config/nvim/plugged

" Dependencies
set rtp^=~/.config/nvim/plugged/vader.vim
set rtp+=./

filetype plugin indent on
